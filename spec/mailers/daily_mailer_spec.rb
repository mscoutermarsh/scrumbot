require "spec_helper"

describe DailyMailer do
  # it "sends email through sidekiq" do
  #   expect { DailyMailer.delay.logs(create(:user))}.to change(Sidekiq::Extensions::DelayedMailer.jobs, :size).by(1)
  # end
end
