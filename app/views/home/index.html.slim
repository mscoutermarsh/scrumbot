.row
  .container
    .col-md-12.text-centered
      h1
        ' scrumlogs
      h2
        ' a daily email of what you got done yesterday
      - if user_signed_in?
        = render 'welcome_back'
      - else
        - cache 'get_started' do
          = render 'get_started'
